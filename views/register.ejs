<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Registration</title>
   <style>
      body {
       font-family: Arial, sans-serif;
       background-color: #1b3a1a;  /* Dark green background */
       margin: 0;
       display: flex;
       justify-content: center;
       align-items: center;
       height: 100vh;
       position: relative;
       flex-direction: column;  /* Allow vertical stacking */
     }

     /* Background Image */
     body::before {
       content: '';
       position: absolute;
       top: 0;
       left: 0;
       width: 100%;
       height: 100%;
       background: url('/image.png') no-repeat center center fixed;
       background-size: cover;
       filter: blur(4px);  /* Increased blur for a softer background */
       z-index: -1;
     }

     .error-message {
       background-color: #f8d7da;
       border: 1px solid #dc3545;
       color: #721c24;
       padding: 10px;
       margin-bottom: 20px;
       border-radius: 5px;
       text-align: center;
       width: 300px;  /* Set a fixed width */
     }

     form {
       background-color: #2a2a2a;  /* Darker gray for the form */
       padding: 30px;
       border-radius: 10px;
       box-shadow: 0 4px 10px rgba(0,0,0,0.5);
       width: 300px;
       color: #fff;  /* White text inside form */
     }

     .form-group {
       margin-bottom: 15px;
     }

     label {
       display: block;
       margin-bottom: 5px;
       color: #a8d5ba;  /* Soft light green text */
       font-weight: bold;
     }

     input {
       width: 100%;
       padding: 10px;
       border: 1px solid #4a7c4a;  /* Darker green border */
       border-radius: 5px;
       box-sizing: border-box;
       background-color: #d5d9d2;  /* Light background for input */
       color: #333;  /* Dark text in input */
       transition: border-color 0.3s ease;
     }

     input:focus {
       border-color: #8dc63f;  /* Bright green border on focus */
       outline: none;  /* Remove default outline */
     }

     button {
       width: 100%;
       padding: 10px;
       background-color: #4caf50;  /* Bright green button */
       color: white;
       border: none;
       border-radius: 5px;
       cursor: pointer;
       transition: background-color 0.3s ease;
     }

     button:hover {
       background-color: #388e3c;  /* Darker green on hover */
     }
   </style>
</head>
<body>
   <% if (messages && messages.length > 0) { %>
     <div class="error-message">
       <% messages.forEach(function(message) { %>
         <p><%= message %></p>
       <% }) %>
     </div>
   <% } %>

   <form action="/register" method="POST">
     <div class="form-group">
       <label for="username">Username</label>
       <input type="text" id="username" name="username" required>
     </div>

     <div class="form-group">
       <label for="email">Email</label>
       <input type="email" id="email" name="email" required>
     </div>

     <div class="form-group">
       <label for="password">Password</label>
       <input type="password" id="password" name="password" required>
     </div>

     <button type="submit">Register</button>
   </form>

   <script src="/script.js"></script>
</body>
</html>